name: negtaskvector
fit_target: forget  # options: [forget, retain]
forget_scaling_coef: 0.6
retain_scaling_coef: 0.0
run_name: BS${training.train_batch_size}_LR${training.learning_rate}_W${training.warmup_ratio}_S${training.seed}
load_from:
  forget: .checkpoints/${model.name}/combined_cc_ss_cp/finetune/${method.run_name}_forget
  retain: .checkpoints/${model.name}/combined_cc_ss_cp/finetune/${method.run_name}_retain
save_model: true

#  python run.py method=negtaskvector do_train=false do_test=true training.world_size=1 training.per_device_batch_size=16 training.gradient_accumulation_steps=1 task=crows_pairs

# defaults:
#   - @_global_.do_train: false
#   - @_global_.do_test: true
#   - @_global_.training.world_size: 1
#   - @_global_.training.per_device_batch_size: 16
#   - @_global_.training.gradient_accumulation_steps: 1
